		var revapi;
		jQuery(document).ready(function() {
				"use strict";
			   revapi = jQuery('.tp-banner').revolution(
				{
					delay:9000,
					startwidth:1170,
					startheight:230,
					hideSliderAtLimit: 480,
					hideThumbs:10,
					fullWidth:"on",
					forceFullWidth:"on"
				});

		});	